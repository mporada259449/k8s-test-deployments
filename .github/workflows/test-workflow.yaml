on:
  push:
    branches:
      - test-workflow

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: First step 
        run: echo "This is first step"
      - name: checkout
        uses: actions/checkout@v4
      - name: print dirs
        run: ls -l